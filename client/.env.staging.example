# Staging build values. Use the real staging HTTPS origins only in the
# deployment platform; these .invalid placeholders are safe for local builds.
VITE_API_URL=https://staging-api.example.invalid/api/v1
VITE_SOCKET_URL=https://staging-api.example.invalid
