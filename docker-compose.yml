version: "3.9"
services:
  api:
    build:
      context: .
      dockerfile: Dockerfile.server
    env_file:
      - server/.env
    ports:
      - "5000:5000"

  web:
    build:
      context: .
      dockerfile: Dockerfile.client
    ports:
      - "8080:80"
    depends_on:
      - api
